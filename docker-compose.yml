services:
  saltspire:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./players:/app/players
    environment:
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=llama3.2:3b
    restart: unless-stopped
